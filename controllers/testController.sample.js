/**
 * Created by SkyAo on 16/5/5.
 *
 * 测试路由（demo）
 *
 * 说明：
 * 路由的前两个参数必须是配置文件的appConfig和appKeys，第三个参数可选，为命令行传入参数。
 * 所有Controller都必须为一个function
 */
module.exports = function(appConfig, appKeys) {
    console.log('hello world');
};